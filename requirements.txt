# Core
requests>=2.31.0
chromadb>=0.5.0

# AST chunking
tree-sitter>=0.21.0
tree-sitter-languages>=1.10.0

# Desktop UI (pure Python, ships as .exe via PyInstaller)
customtkinter>=5.2.2
pillow>=10.0.0          # required by customtkinter for image support

# Optional web API (if you prefer browser access instead)
# fastapi>=0.110.0
# uvicorn[standard]>=0.29.0

python-dotenv>=1.0.0
rich>=13.0.0

# Build to .exe on Windows:
#   pip install pyinstaller
#   pyinstaller app.spec
# Python >= 3.9 required
